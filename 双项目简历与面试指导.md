# 嵌入式双项目 — 简历与面试完整指导

> DataLogger（自主开发）+ 双轮平衡车（教程实现）
> 覆盖 STM32 + FreeRTOS + 控制算法 + 数据可靠性 + 无线通信

---

## 目录

1. [个人技能总结](#1-个人技能总结)
2. [简历版本](#2-简历版本)
3. [平衡车项目分析](#3-平衡车项目分析)
4. [面试开场白](#4-面试开场白)
5. [常见追问速查](#5-常见追问速查)

---

## 1. 个人技能总结

```text
■ MCU/平台：    STM32F103（Cortex-M3），HAL 库 + 标准外设库双经验
■ RTOS：        FreeRTOS（任务设计/队列/互斥锁/事件组/栈溢出检测）
■ 外设总线：    ADC、I2C（硬件+软件模拟）、SPI（硬件+软件模拟）、UART、GPIO、Timer
■ 控制算法：    三环级联 PID、互补滤波、微分先行、Anti-Windup、死区补偿
■ 传感器：      MPU6050 六轴 IMU、NTC 热敏电阻、磁编码器
■ 存储与可靠性：W25Q64 SPI Flash、自定义文件系统、CRC32 校验、A/B 双区冗余、掉电恢复
■ 显示与交互：  SSD1306 OLED（128×64）、三键菜单、蓝牙串口调参
■ 无线通信：    NRF24L01（2.4GHz，双向数据回传）、HC-05 蓝牙
■ 调试工具：    Keil MDK、STM32CubeMX、AixProbe/OpenOCD（寄存器级诊断）
■ 语言：        C（主要）、Python（辅助），构建 0 Error 0 Warning
```

---

## 2. 简历版本

### 2.1 通用版（适合嵌入式软件开发岗）

---

> **DataLogger — 基于 FreeRTOS 的可靠温度采集与日志系统** | 2026.05 – 2026.06
>
> - 基于 **STM32F103C8T6 + FreeRTOS**，集成 NTC 热敏电阻、SSD1306 OLED、W25Q64 SPI Flash
> - 设计 **5 任务多任务架构**（Logger / DataProd / Display / Shell / Monitor），通过队列解耦日志生产与持久化，Flash 互斥锁保护共享存储访问
> - 实现 **逐记录 CRC32 校验与启动恢复**：异常复位后逐条扫描样本记录自动修复文件元数据；配置采用 **A/B 双区 + 版本号 + CRC** 冗余存储，确保任意时刻断电可恢复
> - 在 W25Q64 上实现 **自定义文件系统**（超级块 + 索引表 + 数据区，支持预留空间分配与页安全写入）
> - 实现 **NTC 传感器信号处理链**：16 次过采样去极值 → Beta 模型温度换算 → IIR 低通滤波 → 5 种异常状态分类
> - 在 **20KB SRAM** 约束下完成全部功能（ROM 39KB / RAM 16.4KB，0 Error 0 Warning），通过 AixProbe 完成寄存器级实板验证
>
> **双轮平衡车 — 三环级联 PID 控制系统** | 2025.06 – 2025.08
>
> - 基于 **STM32F103C8T6**（裸机），集成 MPU6050 六轴 IMU、直流减速电机（磁编码器）、SSD1306 OLED
> - 实现 **互补滤波**融合加速度计与陀螺仪数据（α=0.01），获取稳定无漂移俯仰角
> - 实现 **三环级联 PID 控制器**：角度环（100Hz，微分先行）→ 速度环（20Hz）→ 转向环（20Hz），含积分限幅、输出限幅、死区补偿
> - 实现 **NRF24L01 2.4GHz 无线遥控 + 双向数据回传**（32 字节自定义协议，遥测角度/速度/PWM）
> - 实现 **蓝牙串口实时调参**（USART2，自定义 tag-slider-joystick 数据包协议，支持 10 个 PID 参数在线修改）
> - 设计 **安全机制**：跌倒自动停机（±50°）、定时中断超时检测、启动时 PID 状态复位

---

### 2.2 精简版（一页简历，每条一个 bullet）

---

> **嵌入式数据采集系统 (DataLogger)** — STM32F103 + FreeRTOS | 2026.05 – 2026.06
>
> - 5 任务 FreeRTOS 架构，队列解耦 + 互斥锁保护，OLED 三键菜单交互
> - 逐记录 CRC32 校验 + A/B 双区配置冗余 + 启动自动恢复，解决 Flash 掉电数据一致性问题
> - 自定义 W25Q64 文件系统（超级块/索引/数据区），NTC 传感器 16 次过采样 + Beta 模型 + IIR 滤波
> - 20KB SRAM 约束下 0 Error 0 Warning 构建，AixProbe 寄存器级实板验证

> **双轮自平衡车** — STM32F103（裸机）| 2025.06 – 2025.08
>
> - 互补滤波（加速度计+陀螺仪融合）+ 三环级联 PID（角度环/速度环/转向环，微分先行）
> - NRF24L01 无线遥控 + 双向数据回传，蓝牙串口实时 PID 调参
> - 跌倒保护、积分限幅、死区补偿、定时中断超时检测等安全机制

---

### 2.3 侧重可靠性/工控方向

---

> **DataLogger — 高可靠嵌入式数据记录系统**
>
> - 针对 Flash 非原子写入问题，设计**记录级 CRC + 启动逐条扫描恢复**机制，保证异常复位后数据完整性
> - 配置采用 **A/B 双区 + 版本号 + CRC**，遵循先写备份再写主区的安全提交顺序，覆盖全部断电窗口
> - 设计**故障隔离钩子**：栈溢出/内存不足时仅写 volatile 全局变量（不调 UART/队列/互斥锁），避免二次故障掩盖根因
> - 自定义文件系统实现页安全写入与分配冲突检测，W25Q64 全链路 CRC 校验（记录 → 文件 → 超级块）
> - 通过真实断电/复位测试验证恢复流程，Cortex-M3 Fault 寄存器（CFSR/HFSR）全部为零

---

### 2.4 侧重控制/机器人方向

---

> **双轮平衡车 — 三环级联 PID 自平衡系统**
>
> - 基于 MPU6050 六轴 IMU 的**互补滤波姿态解算**，融合加速度计静态精度与陀螺仪动态响应
> - **三环级联 PID**：角度环 100Hz（内环）→ 速度环 20Hz（中环）→ 转向环 20Hz（外环），级联实现站立/行进/转弯
> - 微分先行 + 积分限幅 + 死区补偿，提升目标突变时的平滑性和低速响应
> - NRF24L01 2.4GHz 无线遥控，32 字节自定义协议，支持摇杆控制 + 运行数据实时回传
> - 蓝牙串口实时调参：支持 10 个 PID 参数在线修改，加速控制参数整定

---

## 3. 平衡车项目分析

### 3.1 技术架构

```
裸机中断驱动调度（非 RTOS）：

TIM1 1ms 中断
  ├── 每 1ms  (1000Hz): 按键扫描
  ├── 每 10ms (100Hz):  MPU6050 读取 → 互补滤波 → 角度环 PID → 电机输出
  └── 每 50ms (20Hz):   编码器读取 → 速度环 PID → 转向环 PID

主循环 main():
  └── OLED 显示 + NRF24L01 数据包处理 + 蓝牙数据包处理
```

### 3.2 核心算法

**互补滤波：**
```c
AngleAcc  = -atan2(AX, AZ) / π × 180   // 加速度计（静态准，动态噪声）
AngleGyro = Angle + GY / 32768 × 2000 × 0.01  // 陀螺仪积分（短期精，长期漂）
Angle     = 0.01 × AngleAcc + 0.99 × AngleGyro  // 信任陀螺仪 99%
```

**三环级联 PID 公式（微分先行）：**
```c
Out = Kp×Error + Ki×ErrorInt - Kd×(Actual - Actual1)
```

| 控制环 | 频率 | Kp | Ki | Kd | 输出 | 作用 |
|--------|------|----|----|----|------|------|
| 角度环 | 100Hz | 5 | 0.1 | 5 | AvePWM | 保持直立 |
| 速度环 | 20Hz | 2 | 0.05 | 0 | 角度环目标值 | 前后行进 |
| 转向环 | 20Hz | 4 | 3 | 0 | DifPWM | 左右转弯 |

**电机输出分解：**
```c
LeftPWM  = AvePWM + DifPWM / 2
RightPWM = AvePWM - DifPWM / 2
```

### 3.3 技术亮点

| 亮点 | 说明 | 面试价值 |
|------|------|----------|
| **微分先行** | 对实际值微分而非误差微分 | 理解 PID 变体，不是只会调库 |
| **Ki=0 时清零积分** | 避免调试时积分累积后 Ki 启用的冲击 | 有真实调参经验 |
| **死区补偿** | OutOffset 参数补偿电机启动死区 | 解决过真实控制问题 |
| **软件 SPI/I2C** | NRF24L01/MPU6050 驱动从 GPIO 翻转手写 | 理解通信协议底层 |
| **中断超时检测** | 中断退出前检查标志位，标记超时 | 关心实时性边界 |
| **数据回传协议** | 32 字节自定义包格式，float 直接 memcpy | 理解二进制协议设计 |
| **蓝牙 PID 调参** | 10 个参数在线修改，无需重新编译 | 实用工程思维 |

### 3.4 需坦诚说明的

- 代码框架源自江协科技教程（NRF24L01/MPU6050/PID 驱动），但 PID 参数、互补滤波系数、陀螺仪零漂、中心角偏移均为实测标定
- 裸机中断分频方案适合固定频率控制场景（时序确定性优于 RTOS 任务切换），但不适合复杂异步逻辑
- 无 RTOS、无掉电保护、无文件系统——与 DataLogger 形成互补，合起来覆盖嵌入式两大方向

---

## 4. 面试开场白

### 4.1 1 分钟版本

> "我有两个比较完整的 STM32 项目，一个侧重数据可靠性，一个侧重控制算法。
>
> 第一个是 DataLogger——基于 FreeRTOS 的温度采集和日志系统。核心是做了一条从 NTC 传感器采集到 SPI Flash 持久化存储的完整数据链，每条样本独立 CRC 校验，异常复位后能自动恢复数据。配置用 A/B 双区冗余，确保任何时候断电都能恢复到有效配置。整个系统在 20K SRAM 下跑通，0 Error 0 Warning。
>
> 第二个是双轮平衡车。互补滤波融合 MPU6050 数据做姿态解算，三环级联 PID 实现直立、行进和转弯。微分先行避免目标突变冲击，NRF24L01 无线遥控加数据回传，蓝牙串口做实时 PID 调参。
>
> 这两个项目合起来覆盖了 FreeRTOS 多任务、Flash 存储可靠性、PID 控制和无线通信——基本上嵌入式岗位要求的主要技能都涉及了。"

### 4.2 如果只能选一个说（根据岗位）

| 岗位方向 | 主打项目 | 一句定位 |
|----------|----------|----------|
| 嵌入式软件（通用） | DataLogger | "我设计了一个带掉电恢复的可靠数据采集系统" |
| IoT/智能硬件 | DataLogger | "传感器→存储→显示的完整数据链" |
| 工控/汽车电子 | DataLogger | "从 CRC 校验到双区冗余的工业级可靠性设计" |
| 机器人/运动控制 | 平衡车 | "三环级联 PID + 互补滤波的闭环控制系统" |
| 驱动/电机控制 | 平衡车 | "从 IMU 姿态解算到电机驱动输出的完整控制链" |
| 芯片原厂 AE | 两者都讲 | "HAL 库和标准库都实际用过" |

---

## 5. 常见追问速查

### DataLogger 方向

**Q: 为什么不用 FAT/LittleFS？**
> 项目目标是深入理解 Flash 存储原理——页编程限制、扇区擦除、写入非原子性。自己实现文件系统让我真正理解了为什么 LittleFS 需要事务日志和磨损均衡。生产环境我会优先评估成熟的方案。

**Q: 队列满了怎么办？**
> 生产者投递超时 50ms，超时后递增丢弃计数并返回。OLED 首页显示 Drop 值，用户可以据此调整采样频率。结构化样本走同步写入——样本的语义要求确认落盘才计数。

**Q: 最难修的 bug？**
> DataProd 栈溢出被二次故障掩盖：栈溢出钩子调了 UART → UART 拿 mutex → PendSV 上下文触发 FreeRTOS 断言。修复方案是把故障钩子改成只写 volatile 变量，不调任何可能持锁的服务。同时把栈从 128 words 扩到 256 words。

### 平衡车方向

**Q: 为什么用裸机中断而不是 FreeRTOS？**
> 控制环有严格的定时要求——角度环必须每 10ms 跑一次。硬件定时中断分频能保证绝对的定时精度，比任务切换的 jitter 更小。但代价是所有控制逻辑在中断上下文，计算量受限。DataLogger 那种多速率松耦合的场景就更适合 RTOS。

**Q: 互补滤波的 α 为什么选 0.01？**
> 99% 信任陀螺仪的短期响应，只用 1% 的加速度计修正长期漂移。值越大响应越快但噪声越多，越小越平滑但漂移修正慢。0.01 是实测折中——再大会看到震动噪声，再小漂移修正太慢。

**Q: 微分先行和普通 PID 的区别？**
> 普通 PID 对误差微分——目标值跳变时微分项会产生巨大冲击（Derivative Kick）。微分先行对实际值微分——目标值突变不影响微分项，只影响比例项，输出更平滑。这对平衡车很重要，因为遥控器推摇杆时目标速度会突变。

**Q: 为什么 NRF24L01 用软件 SPI？**
> 一是为了理解 SPI 的 CPOL/CPHA 和 NRF24L01 的寄存器模型——软件实现让你看到每一个时钟沿和数据位。二是 STM32F103 的硬件 SPI 只有 2 个，一个给了 OLED 的 I2C 总线，用软件 SPI 可以灵活分配引脚，不与 OLED 和 MPU6050 的 I2C 冲突。

### 架构与工程方向

**Q: HAL 库和标准库各用过，觉得有什么区别？**
> 标准库更轻量、寄存器操作更直接，适合简单的裸机项目。HAL 抽象层级更高，和 CubeMX 配合好，但占用资源更多，错误处理也更复杂。DataLogger 用 HAL 是因为要和 FreeRTOS 配合，CubeMX 能自动生成时钟树和外设配置。平衡车用标准库是因为教程生态，而且裸机项目不需要 HAL 的抽象层。

**Q: 两个项目如果要量产，各有什么要改进？**
> DataLogger：文件系统换成 LittleFS 获得完整掉电保护；NTC 做两点或三点标定提高精度；加看门狗。平衡车：加 MPU6050 DMP 硬件姿态解算减少 CPU 负担；电机电流检测做堵转保护；遥控器加跳频避免 2.4GHz 干扰。

**Q: 你觉得自己哪个项目更强？**
> DataLogger 是我独立设计的，从架构分层到可靠性设计都是自己思考的结果，代码质量和文档完整度更高。平衡车让我深入理解了 PID 控制和传感器融合，但代码框架是教程的。如果一定要选，DataLogger 更能代表我的工程能力。

**Q: 说一下嵌入式系统的分层思想？**
> DataLogger 里我分了四层：HAL 层只做硬件初始化不关心业务；BSP 层封装外设操作（Flash 读写、按键消抖）但不关心文件名和页面；Service 层实现文件系统、配置管理、UI 状态机等纯逻辑；App 层决定各服务怎么协作（任务分工、IPC、初始化顺序）。每层只依赖下层，不跨层调用。平衡车因为是裸机，分层更扁平，但驱动模块的职责边界也是清晰的。

---

## 附录：两个项目的技术对比总表

| 维度 | DataLogger | 平衡车 |
|------|-----------|--------|
| 来源 | 自主设计 | 教程实现 |
| MCU | STM32F103C8T6 | STM32F103C8 |
| 调度 | FreeRTOS 抢占式 | 裸机中断分频 |
| 库 | HAL | 标准外设库 |
| 核心算法 | CRC32 / Beta 模型 / IIR | 互补滤波 / 级联 PID |
| 传感器 | NTC 热敏电阻 | MPU6050 六轴 IMU |
| 执行器 | 无 | 直流减速电机×2 |
| 显示器 | SSD1306 OLED (I2C) | SSD1306 OLED (软件 I2C) |
| 存储 | W25Q64 (8MB SPI Flash) | 无外部存储 |
| 通信 | UART (调试) | NRF24L01 + HC-05 蓝牙 |
| 输入 | 3 键菜单 | 4 键 + 遥控器 |
| 安全机制 | CRC 校验 / 双区冗余 / 故障隔离 | 跌倒保护 / 超时检测 / 限幅 |
| 构建 | 0 Error 0 Warning | — |
| 文档 | 4 篇完整文档 | 1 篇说明 |
| ROM | 39,488 字节 | — |
| RAM | 16,440 字节 | — |

---

## 简历 checklist

提交简历前检查：

- [ ] 两个项目放在"项目经历"一栏，按时间倒序（DataLogger 在前）
- [ ] 每个项目 4-6 条 bullet，技术关键词加粗或保持清晰
- [ ] 技能栏列出：STM32、FreeRTOS、PID、SPI/I2C/UART、CRC、传感器
- [ ] 简历里写"熟练使用 Keil MDK、STM32CubeMX、OpenOCD/AixProbe 调试"
- [ ] 有 GitHub/Gitee 链接的话附上（即使只有 DataLogger）
- [ ] 准备好"最难 bug"的回答（DataLogger 栈溢出案例）
- [ ] 准备好"项目有什么不足"的回答（主动说已知限制）
- [ ] 如果面试控制岗，带 PID 参数整定的思路
- [ ] 如果面试系统岗，带 FreeRTOS 任务优先级设计的思路
