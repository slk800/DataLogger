# DataLogger 修复日志

> 最后更新：2026-06-11

本文记录从 OLED 主界面、真实 NTC 采集到可靠日志闭环过程中发现的问题、根因、修改和验证结果。

## 2026-06-11：DataProd 任务栈溢出

### 现象

- OLED 能显示，但系统运行后出现停止更新或行为异常。
- AixProbe 多次暂停时，PC 固定在 FreeRTOS 的断言死循环。
- 表面上像 UART 互斥锁或队列异常。

### 根因

FreeRTOS 在 PendSV 切换任务时检测到 `DataProd` 栈溢出，进入 `vApplicationStackOverflowHook()`。旧故障钩子又调用 `BSP_UART_SendString()`，UART 内部尝试获取互斥锁，并在中断上下文进入临界区，触发第二个 FreeRTOS 断言。

因此故障链为：

```text
DataProd 栈溢出
  -> 栈溢出钩子
  -> UART 发送
  -> 获取 UART mutex
  -> ISR/PendSV 上下文进入临界区断言
```

二次断言掩盖了最初的栈溢出。

### 修改

- `TASK_STACK_DATA_PROD` 从 `128 words` 增加到 `256 words`。
- 栈溢出钩子不再调用 UART、队列、互斥锁或动态内存。
- 新增只读故障记录变量：
  - `g_fault_code`
  - `g_fault_task_handle`
  - `g_fault_task_name`
- 内存申请失败钩子采用同样的最小记录策略。

### 验证

- 修复前故障码为 1，任务名为 `DataProd`。
- TCB 显示旧栈顶越过边界约 16 字节。
- 修复后故障码保持 0。
- DataProd 当前处于正常 `vTaskDelay()` 状态。
- 256-word 栈的历史最小剩余约 124 words。

## 2026-06-11：样本计数停在 1

### 现象

- NTC ADC 和温度持续更新。
- DataProd 任务正常周期运行。
- OLED 的有效样本计数长期停在 1。
- `Log_AppendSample()` 从第二条开始持续失败。

### 根因

调试或手动复位发生在样本数据写入后、文件索引提交前。Flash 中第一条 24 字节记录本身有效，但 `sample` 文件索引中的 CRC 保持为 `0xFFFFFFFF`。

后续追加会先校验旧文件 CRC。由于文件级 CRC 与实际数据不匹配，追加被正确拒绝，因此采样正常但持久化计数不再增长。

### 修改

- 新增 `Log_RecoverSampleFile()`：
  - 启动时逐条读取 `SampleRecord_t`。
  - 使用每条记录自身的 CRC 验证连续有效前缀。
  - 遇到空白、CRC 错误或读取错误时停止。
- 新增 `FS_File_RepairPrefix()`：
  - 只接受已经验证的非空有效前缀。
  - 重新计算文件级 CRC。
  - 修正文件逻辑大小和索引元数据。
  - 刷新超级块。
- 恢复逻辑放在 `Log_Init()` 中，在任务启动前完成。

### 验证

- 烧录修复固件后，启动约 16 秒时样本计数达到 15。
- 再运行约 5.5 秒，计数从 15 增长到 23。
- `sample` 文件大小从 360 字节增长到 552 字节。
- 文件 CRC 更新为有效值，故障码仍为 0。

## 2026-06-11：AixProbe 直接烧录

### 现象

项目目录中的旧 `.bin` 生成时间早于最新 AXF，直接烧录存在回退旧固件的风险。

### 处理

- 使用 Keil `fromelf.exe` 从最新 `DataLogger.axf` 导出新的 BIN。
- 验证导出文件大小为 39488 字节。
- 将 BIN 完整上传到 AixProbe。
- 写入 STM32 内部 Flash 地址 `0x08000000`。
- 开启写后校验并复位运行。

### 验证

```text
success: true
wrote: 39488 bytes
verified: true
```

烧录后核心、外设、温度采集和日志追加均正常。

## 2026-06-09 至 2026-06-10：OLED 刷新与按键交互

### 现象

- OLED 上电显示正常，但后续可能停止刷新。
- 刷新过快时难以观察数据。
- 使用杜邦线模拟按键时存在抖动和误触。

### 修改

- OLED 常规刷新周期调整为 1000 ms。
- 按键扫描保持 50 ms，避免降低菜单响应速度。
- 按键加入 30 ms 软件消抖。
- 确认键加入 800 ms 长按识别。
- 按键事件和菜单动作触发 OLED 立即刷新。
- SSD1306/I2C 失败后每 1000 ms 尝试恢复。
- UI 状态通过互斥锁快照传递，避免并发读写不一致。

### 验证

- OLED 可持续刷新温度、Tick 和计数。
- 页面操作不需要等待下一个周期刷新。
- I2C1 空闲时 `BUSY=0`，无错误标志。
- PB12、PB13、PB14 未按下时为高电平，符合内部上拉配置。

## 2026-06-09：真实 NTC 采集替换虚拟数据

### 修改

- 新增 ADC1_IN0/PA0 采集。
- 新增 16 次采样、去极值平均。
- 新增电压、电阻和 B3950 温度换算。
- 新增温度低通滤波。
- 新增 SHORT、OPEN、RANGE、ADC 状态。
- OLED Home/System 页面显示真实温度、状态和 ADC。
- 新增 24 字节 `SampleRecord_t` 和记录 CRC。
- 新增 `sample` 文件和 2 MB 预留追加空间。

### 接线约束

- NTC 模块 VCC 必须优先使用 3.3V。
- AO 接 PA0。
- DO 不连接。
- 蓝色电位器只调节 DO 阈值，不校准 AO 温度。

## 2026-06-09：RAM 链接空间不足

### 现象

Keil 链接时报多个 `L6406E: No space in execution regions`，约有 `0x130` 字节无法放入 RAM。

### 处理

- 关闭默认不用的 UART CLI 和 Monitor 任务。
- 清理未使用缓冲区和缓存状态。
- 保留 OLED 1024 字节帧缓冲和必要的文件系统缓存。
- 按任务实际调用链重新分配栈空间。

### 最终资源占用

```text
Code    36578 bytes
RO-data  2626 bytes
RW-data   284 bytes
ZI-data 16156 bytes
ROM     39488 bytes
RAM     16440 bytes
0 Error(s), 0 Warning(s)
```

## 当前已知限制

- 文件系统元数据不是完整事务日志，当前通过样本逐条 CRC 恢复处理已知断电窗口。
- 文本日志尚未实现与结构化样本相同的逐记录启动恢复。
- 文件索引和超级块更新较频繁，仍可通过批量提交降低擦写次数。
- `sample` 的 2 MB 预留区写满后不会自动覆盖旧样本。
- NTC 使用名义参数，绝对温度精度取决于模块器件误差和标定。
