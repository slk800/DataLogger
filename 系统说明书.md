# DataLogger 系统说明书

> 适用固件：STM32F103C8T6 + SSD1306 I2C OLED + W25Q64 + NTC 热敏电阻模块  
> 文档更新日期：2026-06-11  
> 技术实现见：[技术实现说明.md](技术实现说明.md)  
> 修复历史见：[修复日志.md](修复日志.md)

## 1. 系统定位

DataLogger 是一个本地温度采集、显示和可靠存储系统。系统使用 STM32F103C8T6 作为主控，FreeRTOS 负责多任务调度，0.96 寸 SSD1306 OLED 作为主界面，三枚按键用于菜单操作，W25Q64 用于保存配置、文本日志和结构化温度样本。

当前版本已经使用真实 NTC 热敏电阻模块采集温度，不再使用虚拟温度数据。串口硬件和调试输出仍保留，但串口 CLI 与 Monitor 任务默认关闭，日常使用不需要连接 USB 转串口模块。

主要能力：

- 通过 `PA0/ADC1_IN0` 采集 NTC 模块 AO 模拟电压。
- 对 16 次 ADC 采样去除最大值和最小值后求平均。
- 根据 10K NTC、B3950 模型换算温度，并进行低通滤波。
- OLED 显示温度、采样计数、系统状态、Flash 状态和日志摘要。
- 三键完成页面浏览、采样周期修改、日志等级修改、保存、清除和重启。
- 将每条温度样本以固定结构和独立 CRC32 写入 W25Q64。
- 启动时恢复有效样本数量，并修复被异常复位打断的文件元数据。
- 配置使用 A/B 双区、版本号和 CRC32 保存。
- 保留 SWD/AixProbe 在线调试和固件烧录能力。

## 2. 系统架构

```text
NTC AO
  |
  v
ADC1 / PA0 --> Sensor_NTC_Read() --> UI 状态快照 --> SSD1306 OLED
                       |
                       +--> SampleRecord_t --> sample 文件 --> W25Q64
                       |
                       +--> LogRecord_t --> FreeRTOS 队列 --> log 文件

三枚按键 --> 消抖/长按识别 --> UI 菜单状态机 --> 配置/清除/重启操作
```

设计重点不是横向增加蓝牙、云平台或上位机，而是保证真实采集、本地交互和日志可靠性形成完整闭环。

## 3. 硬件组成

| 模块 | 型号或接口 | 用途 |
| --- | --- | --- |
| 主控 | STM32F103C8T6 | 采集、计算、调度和外设控制 |
| 温度传感器 | 4 线 NTC 热敏电阻模块 | 输出 AO 模拟电压和 DO 阈值信号 |
| 显示器 | 0.96 寸 SSD1306，128x64，I2C | 主界面和菜单 |
| 外部存储 | W25Q64，SPI | 配置、日志和样本存储 |
| 输入 | 3 键：上、下、确认 | 菜单浏览和配置修改 |
| 调试器 | SWD / AixProbe | 烧录、寄存器和运行状态检查 |
| 串口 | USART1，115200-8-N-1 | 可选调试输出和可选 CLI |

所有外部模块必须与 STM32 共地。NTC、OLED 和 W25Q64 推荐统一使用 3.3V 供电。

## 4. 完整接线

### 4.1 NTC 热敏电阻模块

| NTC 模块引脚 | STM32F103 | 说明 |
| --- | --- | --- |
| VCC | 3.3V | 不建议使用 5V，避免 AO 超过 ADC 输入范围 |
| GND | GND | 共地 |
| AO | PA0 | ADC1_IN0，温度计算使用此信号 |
| DO | 不连接 | LM393 阈值输出，当前固件不使用 |

模块上的蓝色电位器主要调节 LM393 的 DO 翻转阈值，不用于 AO 温度标定。当前算法直接使用 AO 分压值。

### 4.2 SSD1306 OLED

| OLED 引脚 | STM32F103 | 说明 |
| --- | --- | --- |
| VCC | 3.3V | OLED 供电 |
| GND | GND | 共地 |
| SCL | PB6 | I2C1_SCL |
| SDA | PB7 | I2C1_SDA |

- 分辨率：128x64。
- I2C 速率：100 kHz。
- 7 位地址：`0x3C`。
- HAL 中使用的地址：`0x3C << 1`。
- 若模块没有板载上拉，可在 SCL、SDA 分别增加 4.7K 至 10K 上拉到 3.3V。

### 4.3 W25Q64

| W25Q64 引脚 | STM32F103 | 说明 |
| --- | --- | --- |
| CS | PB0 | GPIO 片选，高电平空闲 |
| CLK | PA5 | SPI1_SCK |
| DO/MISO | PA6 | SPI1_MISO |
| DI/MOSI | PA7 | SPI1_MOSI |
| VCC | 3.3V | Flash 供电 |
| GND | GND | 共地 |

SPI1 使用主机模式、8 位数据、Mode 0、软件 NSS、MSB first、预分频 8。系统启动时读取 JEDEC ID `0xEF4017` 验证 W25Q64。

### 4.4 三枚按键

| 按键 | STM32F103 | 接法 |
| --- | --- | --- |
| 上 | PB12 | 按下时接 GND |
| 下 | PB13 | 按下时接 GND |
| 确认 | PB14 | 按下时接 GND |

GPIO 使用内部上拉，低电平有效。按键参数：

- 扫描周期：50 ms。
- 软件消抖：30 ms。
- 确认键长按：800 ms。

使用杜邦线模拟按键时，将一根线固定接 GND，另一端短暂接触 PB12、PB13 或 PB14。不要接到 3.3V。

### 4.5 SWD 和可选串口

| 功能 | STM32F103 | 说明 |
| --- | --- | --- |
| SWDIO | PA13 | 调试数据 |
| SWCLK | PA14 | 调试时钟 |
| GND | GND | 调试器与目标板共地 |
| VREF | 3.3V | 目标电平参考 |
| USART1_TX | PA9 | 可选串口发送 |
| USART1_RX | PA10 | 可选串口接收 |

## 5. 时钟和外设配置

系统使用外部 8 MHz 晶振，经 PLL x9 得到 72 MHz 系统时钟：

| 时钟 | 配置 |
| --- | --- |
| SYSCLK | 72 MHz |
| HCLK/AHB | 72 MHz |
| PCLK1/APB1 | 36 MHz |
| PCLK2/APB2 | 72 MHz |
| ADC 时钟 | PCLK2 / 6 = 12 MHz |
| Flash 等待周期 | 2 wait states |
| Flash 预取 | 开启 |

主要外设：ADC1、I2C1、SPI1、USART1、GPIOA/B/C 和 FreeRTOS 时基。

## 6. 软件目录

| 目录 | 职责 |
| --- | --- |
| `App/` | 初始化和 FreeRTOS 任务 |
| `BSP/` | UART、SPI、W25Q64、OLED I2C、按键板级驱动 |
| `Sensor/` | NTC 采样、换算、异常判断和滤波 |
| `OLED/` | SSD1306 帧缓冲和文本绘制 |
| `UI/` | 页面、菜单状态机和线程安全状态快照 |
| `Log/` | 文本日志、结构化样本、CRC 和恢复逻辑 |
| `FS/` | W25Q64 上的简易文件系统 |
| `Config/` | A/B 双区配置持久化 |
| `CLI/` | 可选串口命令行 |
| `Core/` | STM32CubeMX/HAL 初始化代码 |
| `Utils/` | CRC32 和通用工具函数 |
| `MDK-ARM/` | Keil 工程和构建输出 |

## 7. 启动流程

1. HAL 初始化并配置 72 MHz 系统时钟。
2. 初始化 GPIO、ADC1、SPI1、I2C1 和 USART1。
3. `App_Init()` 初始化 UART 调试输出。
4. 初始化 W25Q64，并验证 JEDEC ID。
5. 加载文件系统；未格式化时自动格式化。
6. 确保 `log` 和 `sample` 文件存在。
7. 扫描 `sample` 中连续有效的记录，必要时修复文件大小和文件 CRC。
8. 创建日志队列和系统事件组。
9. 从配置 A/B 区加载有效配置；两区均无效时使用默认值并保存。
10. 校准 ADC，初始化 NTC 滤波状态和 UI 状态。
11. 创建 Logger、DataProd、Display 三个默认任务。
12. 启动 FreeRTOS 调度器。
13. Display 初始化 OLED 和按键，进入 Home 页面。

## 8. FreeRTOS 任务

栈大小单位为 32 位 word。

| 任务 | 优先级 | 栈 | 默认状态 | 职责 |
| --- | ---: | ---: | --- | --- |
| Logger | 3 | 256 words | 开启 | 从队列取文本日志并写入 W25Q64 |
| DataProd | 2 | 256 words | 开启 | NTC 采样、样本追加、UI 状态和日志生成 |
| Display | 2 | 256 words | 开启 | 按键扫描、菜单渲染、OLED 更新和动作执行 |
| Shell | 4 | 256 words | 关闭 | 可选串口 CLI |
| Monitor | 1 | 128 words | 关闭 | 可选串口状态输出 |

Flash 操作由互斥锁保护。UI 状态通过独立互斥锁形成快照，避免显示任务读取到一半更新的数据。

系统启用了栈溢出和内存申请失败钩子。故障钩子只记录故障码、任务句柄和任务名，不调用 UART 或其他可能持锁的服务。

## 9. NTC 温度采集

### 9.1 采样过程

每次温度采集执行以下步骤：

1. 连续读取 16 个 ADC 样本。
2. 删除最大值和最小值。
3. 对剩余 14 个样本求平均，得到 `adc_raw`。
4. 按 3.3V 参考电压换算 `voltage_mv`。
5. 根据分压关系计算 NTC 电阻。
6. 使用 B 参数方程计算摄氏温度。
7. 使用 `7/8` 旧值加 `1/8` 新值的低通滤波更新显示温度。

当前模型参数：

| 参数 | 数值 |
| --- | ---: |
| ADC 满量程 | 4095 |
| 参考电压 | 3300 mV |
| 固定上拉电阻 | 10KΩ |
| NTC 标称阻值 | 10KΩ @ 25°C |
| B 值 | 3950 |
| 合理温度范围 | -20.0°C 至 125.0°C |

分压电阻计算：

```text
Rntc = 10000 * Vout / (3300 - Vout)
```

B 参数温度计算：

```text
1/T = 1/298.15 + ln(Rntc/10000)/3950
TempC = T - 273.15
```

### 9.2 传感器状态

| 状态 | 条件或含义 |
| --- | --- |
| `OK` | ADC 和温度换算正常 |
| `SHORT` | ADC 小于 20，AO 接近 0V |
| `OPEN` | ADC 大于 4075，AO 接近 3.3V |
| `RANGE` | 换算温度超出 -20.0°C 至 125.0°C |
| `ADC` | ADC 启动、转换或读取失败 |

模型参数是常见模块的名义值。若需要更高绝对精度，应使用可靠温度计做两点或三点标定，并修正固定电阻、B 值或温度偏移。

## 10. OLED 页面和操作

OLED 使用 1024 字节帧缓冲。常规刷新周期为 1000 ms；按键事件、保存、清除、重启确认和 OLED 恢复会触发立即刷新。Flash 状态每 2000 ms 更新一次。

OLED 通信失败后，Display 任务每 1000 ms 尝试恢复 SSD1306，避免一次 I2C 异常导致永久停止刷新。

### 10.1 页面

| 页面 | 显示内容 |
| --- | --- |
| Home | 设备名、温度、有效样本数、Tick、日志丢弃数 |
| System | 当前空闲堆、历史最小堆、运行时间、NTC 状态、ADC 原始值 |
| Flash | 已用空间、剩余空间、文件数 |
| Log | 日志等级、队列丢弃数、最近一条文本日志 |
| Config | 采样周期、日志等级、保存配置 |
| Action | 保存配置、清除数据、重启 |

### 10.2 按键识别方式

三个按键均为低电平有效。按键按下时，对应 GPIO 与 GND 导通。

| 操作 | 识别方式 | 说明 |
| --- | --- | --- |
| 上键短按 | PB12 按下后松开 | 页面切换、光标上移或编辑值减小 |
| 下键短按 | PB13 按下后松开 | 页面切换、光标下移或编辑值增大 |
| 确认键短按 | PB14 按下后在 800 ms 内松开 | 进入编辑、接受当前值或执行菜单项 |
| 确认键长按 | PB14 持续按住至少 800 ms | 返回 Home，或在清除确认页执行清除 |

短按事件在按键松开时产生。因此使用杜邦线模拟按键时，应接触约 0.1 至 0.3 秒后再松开；只快速碰一下可能达不到 30 ms 消抖时间。长按确认键时，看到界面跳转后即可松开，松开不会再产生一次短按。

### 10.3 页面导航

Home、System、Flash 和 Log 属于普通状态页面。在这些页面按上或下，会切换到相邻页面：

```text
Home -> System -> Flash -> Log -> Config
```

页面按环形方式处理。在 Home 页面按一次上键，会直接进入 Action 页面，这是进入操作菜单最快的方法。

需要注意：进入 `Config` 或 `Action` 后，上/下键改为移动页面内的 `>` 光标，不再继续切换页面。要离开这两个页面，可长按确认键返回 Home。

| 当前页面 | 上/下键 | 确认短按 | 确认长按 |
| --- | --- | --- | --- |
| Home/System/Flash/Log | 切换页面 | 无动作 | 返回 Home |
| Config | 在 Sample、Level、Save 间移动光标 | 编辑或保存当前项 | 返回 Home |
| Action | 在 Save Config、Erase Data、Reboot 间移动光标 | 执行或进入确认 | 返回 Home |

### 10.4 查看实时数据

上电后默认进入 Home 页面：

```text
DataLogger
Temp:27.4 C
Cnt :23
Tick:25660
Drop:0
```

- `Temp`：经过滤波的 NTC 温度。传感器异常时显示 `SENSOR ERR`。
- `Cnt`：已经成功写入 `sample` 文件的有效样本数，不只是 ADC 读取次数。
- `Tick`：系统运行毫秒数。
- `Drop`：文本日志队列发送失败的累计次数。

按下键依次查看：

1. `System`：堆空间、运行时间、NTC 状态和 ADC 原始值。
2. `Flash`：Flash 已用空间、剩余空间和有效文件数。
3. `Log`：当前日志等级、丢弃数和最近一条文本日志。
4. `Config`：采样周期、日志等级和保存入口。

### 10.5 修改采样周期

可选采样周期为：`100`、`200`、`500`、`1000`、`2000`、`5000 ms`。

操作步骤：

1. 从 Home 连续按下键，进入 `Config` 页面。
2. 确认光标位于 `Sample`：屏幕显示类似 `> Sample:1000ms`。
3. 短按确认键进入编辑模式，底部显示 `Edit:UP/DN OK`。
4. 按上键选择更短的周期，按下键选择更长的周期。
5. 短按确认键接受当前周期并退出编辑模式。
6. 按下键两次，将光标移动到 `Save`。
7. 短按确认键写入配置 A/B 区。

第 5 步后新周期会立即影响当前运行；但如果没有执行 `Save`，复位或断电后会恢复为上一次保存的配置。

### 10.6 修改日志等级

日志等级顺序为：

```text
DEBUG -> INFO -> WARN -> ERROR
```

数值越靠后，保留的日志越少。操作步骤：

1. 进入 `Config` 页面。
2. 用上/下键将光标移动到 `Level`。
3. 短按确认键进入编辑模式。
4. 按上键降低等级，按下键提高等级。
5. 短按确认键接受当前等级。
6. 将光标移动到 `Save`，短按确认完成持久化。

日志等级修改后立即作用于当前运行；同样需要执行 `Save` 才能在断电后保留。

### 10.7 保存配置

保存配置有两个入口：

- `Config -> Save`
- `Action -> Save Config`

两者效果相同，都会调用配置保存流程，将采样周期、日志等级、设备名等当前配置写入 W25Q64 的 A/B 双区。

保存操作没有单独的完成弹窗。短按确认后界面会立即刷新；可以通过复位系统并检查设置是否保留来验证保存结果。

### 10.8 清除全部数据

`Erase Data` 会格式化文件系统并删除文本日志和全部结构化温度样本，操作不可撤销。配置 A/B 区不在文件系统数据区内，因此采样周期和日志等级配置会保留。

操作步骤：

1. 在 Home 页面按一次上键，进入 `Action`。
2. 按下键一次，将光标移动到 `Erase Data`。
3. 短按确认键，进入 `Erase all data?` 确认页。
4. 若要取消，短按上键或下键，返回 Action 页面。
5. 若确认清除，持续按住确认键至少 800 ms。
6. 系统格式化文件系统，重新创建 `log` 和 `sample` 文件，并通知 DataProd 重新加载计数。
7. 返回 Home 后观察 `Cnt` 重新开始增长。

确认页中的确认键短按不会执行清除，必须长按。这用于避免误操作。

### 10.9 重启系统

操作步骤：

1. 在 Home 页面按一次上键进入 `Action`。
2. 将光标移动到 `Reboot`。
3. 短按确认键。

系统会先保存当前配置，再调用软件复位。复位后 OLED 重新初始化，系统扫描并恢复 `sample` 文件中的有效样本计数。

### 10.10 快速操作表

| 目的 | 最短操作路径 |
| --- | --- |
| 查看系统状态 | Home -> 下 -> System |
| 查看 Flash | Home -> 下 -> 下 -> Flash |
| 查看最近日志 | Home -> 下 -> 下 -> 下 -> Log |
| 修改采样周期 | Home -> 连按下至 Config -> 确认 -> 上/下 -> 确认 -> 选择 Save -> 确认 |
| 修改日志等级 | Config -> 选择 Level -> 确认 -> 上/下 -> 确认 -> 选择 Save -> 确认 |
| 保存配置 | Home -> 上 -> Action/Save Config -> 确认 |
| 清除数据 | Home -> 上 -> Action/Erase Data -> 确认 -> 长按确认 |
| 重启系统 | Home -> 上 -> Action/Reboot -> 确认 |
| 从任意菜单返回首页 | 长按确认约 0.8 秒 |

### 10.11 操作异常排查

- 按键无响应：确认按键是接 GND，而不是接 3.3V；接触时间应超过 30 ms。
- 按一下却没有动作：短按事件在松开时产生，确保已经松开杜邦线。
- 总是在页面内移动光标：当前位于 Config 或 Action，长按确认返回 Home。
- 误进入清除确认页：按上或下即可取消，不要长按确认。
- 修改后重启失效：只接受了编辑值，但没有执行 `Save` 或 `Save Config`。
- 显示更新较慢：正常数据刷新周期为 1 秒；按键事件会触发立即刷新。

## 11. 配置持久化

默认配置：

| 参数 | 默认值 |
| --- | --- |
| 采样周期 | 1000 ms |
| 日志等级 | INFO |
| 最大日志条数参数 | 500 |
| 设备名 | DataLogger |

配置 A 区位于 `0x004000`，B 区位于 `0x006000`。每份配置包含魔数 `0xCAFEBABE`、版本号和 CRC32。

保存顺序：

1. 版本号递增并计算 CRC。
2. 擦除并写入 B 区。
3. 回读验证 B 区。
4. B 区有效后擦除并写入 A 区。
5. 回读验证 A 区。

启动时选择有效且版本较新的副本；只有一个副本有效时，用它修复另一个副本。

## 12. 日志和样本

### 12.1 文本日志 `log`

文本日志用于人类阅读，包含时间戳、等级、长度、文本和 CRC。DataProd 将日志放入长度为 16 的 FreeRTOS 队列，Logger 任务异步追加到 `log` 文件。

队列发送超时为 50 ms。队列满时增加 `g_log_dropped`，OLED 的 `Drop` 字段显示该计数。

### 12.2 结构化样本 `sample`

每条真实采样使用 24 字节固定结构：

```c
typedef struct {
    uint32_t timestamp;
    uint32_t sample_count;
    int32_t  temperature_x10;
    uint16_t adc_raw;
    uint16_t voltage_mv;
    uint8_t  ntc_status;
    uint8_t  reserved[3];
    uint32_t crc;
} SampleRecord_t;
```

`temperature_x10=274` 表示 27.4°C。每条记录拥有独立 CRC32，因此可以逐条判断数据是否有效。

`sample` 首次写入时预留 2 MB 连续空间，约可保存：

```text
2 * 1024 * 1024 / 24 = 87381 条样本
```

在 1 秒采样周期下约为 24.3 小时；5 秒周期下约为 5.1 天。空间写满后当前实现返回空间不足，不自动覆盖旧样本。

### 12.3 异常复位恢复

Flash 追加包含“写数据”和“更新索引元数据”两个阶段。若在两者之间掉电或复位，数据本体可能有效，但文件 CRC 尚未提交。

启动恢复流程：

1. 读取 `sample` 的逻辑大小。
2. 从第一条开始逐条读取 `SampleRecord_t`。
3. 以记录内 CRC 验证连续有效前缀。
4. 对有效前缀重新计算文件 CRC。
5. 必要时更新文件大小和 CRC，并刷新超级块。
6. DataProd 从有效样本数量继续计数和追加。

恢复不会把 CRC 错误记录当作有效记录，也不会自动格式化整个 Flash。

## 13. W25Q64 空间布局

| 地址范围 | 用途 |
| --- | --- |
| `0x000000-0x000FFF` | 文件系统超级块 |
| `0x001000-0x003FFF` | 文件索引表 |
| `0x004000-0x005FFF` | 配置 A 区 |
| `0x006000-0x007FFF` | 配置 B 区 |
| `0x008000-0x7FFFFF` | 文件数据区 |

Flash 页大小为 256 字节，扇区大小为 4 KB。驱动支持读、页编程、4 KB 擦除、64 KB 擦除和全片擦除。

当前文件系统为项目自定义简易实现，不等同于 FAT/LittleFS。频繁更新索引和超级块会产生额外擦写，长时间高频采样仍有进一步批量提交和磨损均衡的优化空间。

## 14. 清除数据

在 Action 页面选择 `Erase Data` 后进入确认页，长按确认键才会执行：

1. 获取 Flash 互斥锁。
2. 格式化文件系统元数据区。
3. 重新加载索引缓存。
4. 重新创建 `log` 和 `sample` 文件。
5. 通知 DataProd 重新读取样本计数。

此操作会清除已保存日志和结构化样本，无法撤销。

## 15. 编译和烧录

Keil 工程：

```text
F:\ARM_ST_AI\DataLogger\MDK-ARM\DataLogger.uvprojx
```

命令行编译：

```powershell
$env:Path = 'E:\Keil5\Core\UV4;' + $env:Path
& 'E:\Keil5\Core\UV4\UV4.exe' -b `
  'F:\ARM_ST_AI\DataLogger\MDK-ARM\DataLogger.uvprojx'
```

最新构建结果：

```text
Program Size: Code=36578 RO-data=2626 RW-data=284 ZI-data=16156
Total ROM Size: 39488 bytes
Total RW Size: 16440 bytes
0 Error(s), 0 Warning(s)
```

最新 HEX：

```text
F:\ARM_ST_AI\DataLogger\MDK-ARM\DataLogger\DataLogger.hex
```

AixProbe 已验证可将最新 AXF 导出为 BIN，写入 `0x08000000` 并执行写后校验。使用其他工具烧录时选择 STM32F103C8T6、SWD 接口和上述 HEX 文件即可。

## 16. 推荐测试流程

### 16.1 上电检查

1. 检查所有模块均使用 3.3V 并共地。
2. 上电或复位，确认 OLED 进入 Home 页面。
3. 等待 5 秒，确认 `Tick` 和 `Cnt` 增长。
4. 确认 System 页 `NTC:OK`，ADC 不接近 0 或 4095。
5. 确认 Flash 页文件数和容量可读。

### 16.2 NTC 测试

1. 记录室温显示值。
2. 用手捏住 NTC 探头，观察温度缓慢上升。
3. 松开后观察温度逐渐回落。
4. 断开 AO，应显示传感器异常或 OPEN。
5. 将 AO 接近 GND，应显示 SHORT。

### 16.3 按键和菜单测试

1. 用上、下键循环浏览六个页面。
2. 在 Config 修改采样周期并确认。
3. 观察 `Cnt` 增长速度是否随周期改变。
4. 修改日志等级并保存。
5. 复位后确认配置仍保持。

### 16.4 日志恢复测试

1. 让系统运行，确认 `Cnt` 持续增长。
2. 复位目标板。
3. 确认计数从 Flash 中已有有效样本数继续，而不是从 0 开始。
4. 再运行数秒，确认计数继续增加。
5. 进入 Flash 页确认已用空间增长。

### 16.5 清除测试

1. 进入 Action -> Erase Data。
2. 用上/下键验证可以取消。
3. 再次进入并长按确认。
4. 返回 Home，确认样本计数重新开始。

## 17. 常见问题

### OLED 不显示

- 检查 VCC、GND、PB6、PB7。
- 检查地址是否为 `0x3C`。
- 缩短杜邦线，检查 I2C 上拉。
- 确认 OLED 模块确实为 I2C 版本。

### OLED 初始正常但不再刷新

- 检查 SCL/SDA 接触和供电稳定性。
- 避免按键杜邦线误碰 PB6/PB7。
- 固件会每秒尝试恢复 OLED；持续失败通常是硬件连线问题。

### 屏幕刷新太快或太慢

修改 `App/task_display.c` 中的：

```c
#define OLED_REFRESH_MS 1000U
```

不建议同时增大 `KEY_SCAN_PERIOD_MS`，否则按键响应会明显变慢。

### 温度偏差较大

- 确认模块供电为 3.3V。
- 检查实际 NTC 是否为 10K/B3950。
- 模块上的固定电阻可能并非精确 10K。
- 使用参考温度计标定，不要用 DO 电位器校准 AO 温度。

### 样本计数不增长

- 查看 System 页 NTC 状态。
- 查看 Flash 页文件状态。
- 确认 W25Q64 接线和 JEDEC ID。
- 若数据已不需要，可执行 Erase Data 重建文件系统。
- 调试时检查故障码是否为 0，以及 DataProd 是否处于延时或运行状态。

### 串口没有交互

这是默认配置。`APP_ENABLE_UART_CLI=0`、`APP_ENABLE_UART_MONITOR=0`，OLED 才是主界面。USART1 初始化和部分调试输出仍保留。

## 18. 当前边界

- 温度模型使用名义 10K/B3950 参数，尚未做产品级标定。
- NTC 模块 DO 阈值输出未接入。
- `sample` 预留区写满后不会自动覆盖旧数据。
- 自定义文件系统尚未实现完整事务日志和磨损均衡。
- 文本日志和样本元数据仍有擦写优化空间。
- OLED 为英文 6x8 文本界面，未加入中文字库。
- CLI 和 Monitor 默认关闭，系统定位为本地独立运行。

## 19. 最终验证状态

当前固件已经完成以下实板验证：

- AixProbe 直接烧录 39488 字节固件，写后校验通过。
- 故障码为 0，CFSR/HFSR 无 Fault 标志。
- DataProd 栈历史最小剩余约 124 words。
- 约 5.5 秒内有效样本计数从 15 增长到 23。
- `sample` 文件大小从 360 字节增长到 552 字节，文件 CRC 正常更新。
- NTC 实测状态为 OK，曾读取约 27°C 的有效室温数据。
- RCC、GPIO、FLASH、ADC、I2C1 和 SPI1 寄存器状态与配置一致。
- CPU 已恢复运行，OLED、采集和日志任务持续工作。
